import globalObjects from '../../../utils/global-objects';
import { module, test } from 'qunit';

module('Unit | Utility | global objects');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = globalObjects();
  assert.ok(result);
});
